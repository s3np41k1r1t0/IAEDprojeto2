#include "estruturas.h"

/*
jogo* cria_jogo(char* nome, int size_nome, char* equipa1, int size1, char* equipa2, int size2, int score1, int score2){
    jogo* novo = NULL;
    
    jogo->nome = (char*)malloc(sizeof(char)*size_nome);
    jogo->equipa1 = (char*)malloc(sizeof(char)*size1);
    jogo->equipa2 = (char*)malloc(sizeof(char)*size2);
    jogo->score1 = score1;
    jogo->score2 = score2;

    strncpy(jogo->nome,nome,size_nome);
    strncpy(jogo->equipa1,equipa1,size1);
    strncpy(jogo->equipa2,equipa2,size2);

    return novo;
}

void init(int size){
    size_table = size;
    heads = (link*)calloc()
}
*/


